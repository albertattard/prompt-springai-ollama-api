spring:
  main:
    web-application-type: none
  ai:
    model:
      chat: ollama
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: llama2

logging:
  level:
    demo: TRACE
    org.zalando.logbook: TRACE
